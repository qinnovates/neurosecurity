name: Publish qtara

on:
  push:
    branches: [main]
    paths:
      - 'datalake/qtara-registrar.json'
      - 'datalake/qtara/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build qtara
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4

    - name: Set up Python
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      with:
        python-version: "3.11"

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Sync TARA data
      run: python datalake/scripts/sync-package-data.py

    - name: Build binary wheel and source tarball
      run: |
        cd datalake/qtara
        python -m build

    - name: Check package
      run: |
        cd datalake/qtara
        twine check dist/*

    - name: Upload artifacts
      uses: actions/upload-artifact@v7 # TODO: pin to SHA
      with:
        name: dist-qtara
        path: datalake/qtara/dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/qtara
    permissions:
      id-token: write
      contents: write

    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4

    - name: Download artifacts
      uses: actions/download-artifact@v8 # TODO: pin to SHA
      with:
        name: dist-qtara
        path: dist/

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@76f52bc884231f62b54e755bed48b62a5f9e3608 # release/v1
      with:
        packages-dir: dist/
        skip-existing: true

    - name: Extract version
      id: version
      run: |
        pip install tomllib 2>/dev/null || true
        VERSION=$(python3 -c "import tomllib; print(tomllib.load(open('datalake/qtara/pyproject.toml','rb'))['project']['version'])")
        echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        echo "tag=qtara-v$VERSION" >> "$GITHUB_OUTPUT"

    - name: Create GitHub Release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        TAG="${{ steps.version.outputs.tag }}"
        VERSION="${{ steps.version.outputs.version }}"

        if gh release view "$TAG" > /dev/null 2>&1; then
          echo "Release $TAG already exists, skipping"
          exit 0
        fi

        gh release create "$TAG" \
          --title "qtara v${VERSION}" \
          --notes "## qtara v${VERSION}

        TARA threat registry and neural impact scoring for BCI security.

        ### Install
        \`\`\`bash
        pip install qtara==${VERSION}
        \`\`\`

        ### PyPI
        https://pypi.org/project/qtara/${VERSION}/
        " \
          dist/*
