---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import BciVisionToggle from '../components/BciVisionToggle';
import FloatingOrbs from '../components/FloatingOrbs';
import KinectVision from '../components/KinectVision';
import ScrollCounter from '../components/ScrollCounter';
import '../styles/global.css';
import registry from '@shared/qtara-registrar.json';

const techniques = (registry as any).techniques ?? [];
const totalThreats = techniques.length;
const therapeuticCount = techniques.filter((t: any) => t?.tara?.clinical?.therapeutic_analog).length;
const dualUsePercent = Math.round((therapeuticCount / totalThreats) * 100);
---

<BaseLayout title="BCI Vision — Qinnovate" description="Nobody should go through what I lived through. I will not stop until I build a world where we can feel safe." ogImage="/images/og-bci-vision.png">
  <Nav />

  <!-- Scroll progress indicator -->
  <div id="scroll-progress" class="fixed top-0 left-0 h-[2px] z-50" style="width: 0%; background: linear-gradient(to right, #3b82f6, #8b5cf6, #22c55e);"></div>
  <script is:inline>
    (function() {
      var bar = document.getElementById('scroll-progress');
      window.addEventListener('scroll', function() {
        var scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
        var scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        var pct = scrollHeight > 0 ? (scrollTop / scrollHeight) * 100 : 0;
        bar.style.width = pct + '%';
      });
    })();
  </script>

  <script is:inline>
    document.documentElement.setAttribute('data-theme', 'dark');
    document.documentElement.setAttribute('data-vision', 'on');
  </script>

  <FloatingOrbs client:idle />

  <!-- Consent Gate -->
  <div id="vision-consent" class="fixed inset-0 z-[200] flex items-center justify-center" style="background: radial-gradient(ellipse at center, #0a0f0d 0%, #050805 100%);">
    <div class="max-w-lg mx-auto px-6 text-center">
      <div class="mb-8">
        <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-[rgba(139,92,246,0.3)] mb-6" style="background: rgba(139,92,246,0.08);">
          <span class="w-2 h-2 rounded-full bg-violet-500 animate-pulse"></span>
          <span class="text-[10px] font-mono tracking-[0.2em] uppercase text-violet-400">Entering Theoretical Space</span>
        </div>
        <h2 class="text-2xl sm:text-3xl font-bold text-white mb-6 leading-snug">Before you step in</h2>
        <div class="space-y-4 text-sm text-zinc-400 leading-relaxed text-left">
          <p>What you're about to see is an <span class="text-white font-medium">active investigation</span>, not a finished product. This vision reflects where BCI security research leads when you follow the physics.</p>
          <p>If you are a BCI patient, we want to hear from you. It's for you, after all.</p>
        </div>
      </div>
      <button id="consent-btn" class="px-8 py-3 rounded-xl text-sm font-semibold transition-all cursor-pointer" style="background: rgba(139,92,246,0.3); color: #a78bfa; border: 1px solid rgba(139,92,246,0.3);">
        I understand &mdash; let me explore
      </button>
    </div>
  </div>

  <script is:inline>
    (function() {
      var consent = document.getElementById('vision-consent');
      var btn = document.getElementById('consent-btn');
      // First-visit only — localStorage persists across sessions
      if (localStorage.getItem('vision-consent-seen') === 'yes') {
        if (consent) consent.style.display = 'none';
        return;
      }
      btn.onmouseenter = function() { btn.style.background = 'rgba(139,92,246,0.5)'; };
      btn.onmouseleave = function() { btn.style.background = 'rgba(139,92,246,0.3)'; };
      btn.onfocus = function() { btn.style.background = 'rgba(139,92,246,0.5)'; btn.style.outline = '2px solid #a78bfa'; btn.style.outlineOffset = '2px'; };
      btn.onblur = function() { btn.style.background = 'rgba(139,92,246,0.3)'; btn.style.outline = 'none'; };
      btn.addEventListener('click', function() {
        localStorage.setItem('vision-consent-seen', 'yes');
        consent.style.transition = 'opacity 0.4s ease';
        consent.style.opacity = '0';
        setTimeout(function() { consent.style.display = 'none'; }, 400);
      });
    })();
  </script>


  <!-- ═══ MISSION + KINECTVISION HEROES ═══ -->
  <section class="relative py-20 sm:py-28">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center mb-16">
      <p class="text-xl sm:text-2xl text-[var(--color-text-muted)] leading-relaxed mb-10 reveal-elastic">Nobody should go through what I lived through.</p>

      <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold leading-[1.25] mb-12 reveal-elastic" style="transition-delay: 150ms;">
        <span class="text-[var(--color-text-primary)]">I will not stop until I build a world where we can feel safe</span> —
        <span class="bg-gradient-to-r from-emerald-500 via-[var(--color-accent-secondary)] to-[var(--color-accent-tertiary)] bg-clip-text text-transparent">where our children don't need to live in fear.</span>
      </h1>

      <div class="space-y-6 text-lg text-[var(--color-text-muted)] leading-relaxed text-left max-w-2xl mx-auto">
        <p class="reveal-elastic" style="transition-delay: 300ms;">A blind person should be able to see their service dog.</p>
        <p class="reveal-elastic" style="transition-delay: 400ms;">A child should never be invisible to the systems designed to protect them.</p>
        <p class="reveal-elastic" style="transition-delay: 500ms;">The same framework that secures a neural interface can fight human trafficking — starting with the children — while respecting compliance frameworks like COPPA that exist to keep them safe.</p>
        <p class="reveal-elastic text-base" style="transition-delay: 600ms;">But vision restoration is just the beginning. {therapeuticCount} of {totalThreats} attack techniques have therapeutic counterparts. Blindness. Parkinson's. Depression. Tinnitus. Dementia. Every one of these conditions has a security counterpart in the atlas — and every one of them deserves the same protection.</p>
        <p class="reveal-elastic text-base" style="transition-delay: 700ms;">That's why this framework is open source. Not because it's finished — because it can't be finished by one person. The world needs to explore what's here.</p>
        <p class="reveal-elastic text-center text-xl font-semibold text-[var(--color-text-primary)]" style="transition-delay: 800ms;">Help those who need us the most, first.<br/>I will never lose sight of that north star.</p>
        <p class="reveal-elastic text-center mt-4" style="transition-delay: 900ms;"><a href="/research/papers/2026-02-06-field-journal-004-the-all-nighter-seven-layers-neural-protocols-and-the-vision/" class="text-xs text-[var(--color-accent-primary)] hover:underline">Read the all-night session that started this &rarr;</a></p>
      </div>
    </div>

    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-2 gap-6">
        <!-- Vision Restoration hero -->
        <div>
          <div class="rounded-xl overflow-hidden border border-[rgba(34,197,94,0.2)] cursor-pointer transition-all hover:border-emerald-500/50 hover:shadow-[0_0_30px_rgba(34,197,94,0.1)]" data-hero="green" role="button" tabindex="0" aria-label="Expand Vision Restoration details">
            <KinectVision client:only="react" variant="green" />
          </div>
          <div class="mt-4 text-center">
            <p class="text-sm font-semibold text-emerald-500 mb-1">Vision Restoration</p>
            <p class="text-xs text-[var(--color-text-faint)]">Depth sensor captures the environment, AI reconstructs color. Click to explore the pipeline.</p>
          </div>
        </div>
        <!-- Neural Terminal hero -->
        <div>
          <div class="rounded-xl overflow-hidden border border-[rgba(59,130,246,0.2)] cursor-pointer transition-all hover:border-blue-500/50 hover:shadow-[0_0_30px_rgba(59,130,246,0.1)]" data-hero="white" role="button" tabindex="0" aria-label="Expand Neural Terminal details">
            <KinectVision client:only="react" variant="white" />
          </div>
          <div class="mt-4 text-center">
            <p class="text-sm font-semibold text-blue-500 mb-1">Neural Terminal</p>
            <p class="text-xs text-[var(--color-text-faint)]">White phosphenes as the universal BCI interface. Click to explore the architecture.</p>
          </div>
        </div>
      </div>

      <!-- ═══ EXPANDED: Vision Restoration ═══ -->
      <div id="detail-green" class="hidden mt-8 glass rounded-2xl overflow-hidden" style="border-color: rgba(34,197,94,0.2);">
        <div class="grid md:grid-cols-[1.2fr_1fr] gap-0">
          <!-- Left: larger render -->
          <div class="p-4 sm:p-6">
            <div class="rounded-xl overflow-hidden border border-[rgba(34,197,94,0.15)]">
              <KinectVision client:only="react" variant="green" />
            </div>
          </div>
          <!-- Right: explanation -->
          <div class="p-6 sm:p-8 border-t md:border-t-0 md:border-l border-[rgba(34,197,94,0.1)]">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-lg font-bold text-emerald-500">How Vision Restoration Works</h3>
              <button data-close="green" class="text-[var(--color-text-faint)] hover:text-[var(--color-text-primary)] transition-colors cursor-pointer" aria-label="Close">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" /></svg>
              </button>
            </div>

            <div class="space-y-5 text-sm text-[var(--color-text-muted)] leading-relaxed">
              <!-- Sensor layer -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
                  <p class="text-xs font-semibold text-emerald-400 uppercase tracking-wider">Depth Sensor — The Hardware Failsafe</p>
                </div>
                <p>Structured-light depth sensors — the same technology in Xbox Kinect, Meta Quest, and iPhone LiDAR — capture the 3D environment as a point cloud. This runs at the hardware level with no AI dependency. If every software layer above fails, the sensor still produces raw depth data. The patient never loses spatial awareness.</p>
                <p class="mt-2">The sensor is built into the <strong class="text-[var(--color-text-primary)]">system-level kernel</strong> at I0 — it cannot be disabled by software, providing emergency terminal access regardless of application state.</p>
              </div>

              <!-- AI layer -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-violet-500"></span>
                  <p class="text-xs font-semibold text-violet-400 uppercase tracking-wider">AI Color Reconstruction — Local Only</p>
                </div>
                <p>AI renders color and object recognition on top of the depth data using on-device computer vision (OCR for text, object classification for scene understanding). All processing stays local. No frames leave the device. Only hashed metadata — never raw imagery — is transmitted, encrypted via post-quantum key encapsulation (ML-KEM-768) through NSP.</p>
              </div>

              <!-- Runemate -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-amber-500"></span>
                  <p class="text-xs font-semibold text-amber-400 uppercase tracking-wider">Runemate — The Rendering Engine</p>
                </div>
                <p>Runemate compiles the fused sensor + AI output into a neural-optimized stream for cortical delivery. It compresses the visual data to fit within the bandwidth and power constraints of an implanted BCI — targeting 65-90% compression in simulation. The compiler runs on-device at S2, producing phosphene-mapped stimulation patterns the visual cortex can interpret.</p>
              </div>

              <!-- NSP -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-cyan-500"></span>
                  <p class="text-xs font-semibold text-cyan-400 uppercase tracking-wider">NSP — Post-Quantum Security Envelope</p>
                </div>
                <p>Every packet between S2 (Runemate) and I0 (Neurowall) is wrapped in NSP's five defense layers. What leaves the rendering pipeline is cryptographically authenticated — if a packet is modified in transit, Neurowall rejects it at the hardware boundary before it reaches neural tissue.</p>
              </div>

              <!-- Security implications -->
              <div class="pt-4 border-t border-[rgba(34,197,94,0.1)]">
                <p class="text-xs font-semibold text-[var(--color-text-primary)] uppercase tracking-wider mb-2">Security Implications</p>
                <ul class="space-y-2 text-xs">
                  <li class="flex items-start gap-2">
                    <span class="text-emerald-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Emergency terminal access</strong> — depth sensor renders a minimal spatial interface at the kernel level, independent of all software. If Runemate crashes, NSP is compromised, or AI hallucinates, the patient retains basic spatial vision through raw sensor data</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-emerald-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Blockchain configuration versioning</strong> — every kernel configuration state is hashed to a tamper-proof chain. If an accidental misconfiguration occurs, the system can roll back to any previous verified state — version control for the hardware boundary, not just source code</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-emerald-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Zero raw data exfiltration</strong> — visual frames never leave the device. NSP transmits only hashed metadata (scene classification, object counts, navigation events) encrypted with post-quantum key encapsulation. No reconstructable imagery crosses the wire</span>
                  </li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- ═══ EXPANDED: Neural Terminal ═══ -->
      <div id="detail-white" class="hidden mt-8 glass rounded-2xl overflow-hidden" style="border-color: rgba(59,130,246,0.2);">
        <div class="grid md:grid-cols-[1.2fr_1fr] gap-0">
          <!-- Left: larger render -->
          <div class="p-4 sm:p-6">
            <div class="rounded-xl overflow-hidden border border-[rgba(59,130,246,0.15)]">
              <KinectVision client:only="react" variant="white" />
            </div>
          </div>
          <!-- Right: explanation -->
          <div class="p-6 sm:p-8 border-t md:border-t-0 md:border-l border-[rgba(59,130,246,0.1)]">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-lg font-bold text-blue-500">The Neural Terminal</h3>
              <button data-close="white" class="text-[var(--color-text-faint)] hover:text-[var(--color-text-primary)] transition-colors cursor-pointer" aria-label="Close">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" /></svg>
              </button>
            </div>

            <div class="space-y-5 text-sm text-[var(--color-text-muted)] leading-relaxed">
              <!-- Terminal concept -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-blue-500"></span>
                  <p class="text-xs font-semibold text-blue-400 uppercase tracking-wider">Why Terminals Persist</p>
                </div>
                <p>CLI survived GUI. GUI survived mobile. Mobile survived spatial computing. Across every paradigm shift, the terminal persists — a minimal, reliable rendering surface that gives users direct control. White phosphene stimulation is the simplest, most power-efficient rendering primitive for cortical prosthetics. The neural terminal is the BCI equivalent of the command line.</p>
              </div>

              <!-- Rendering -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-violet-500"></span>
                  <p class="text-xs font-semibold text-violet-400 uppercase tracking-wider">Runemate — Terminal Rendering</p>
                </div>
                <p>Runemate compiles interactive content into phosphene-mapped stimulation patterns. Text, UI elements, navigation — all translated into the spatial encoding the visual cortex can process. The patient interacts with their AI, their data, their device configuration through a terminal they control.</p>
              </div>

              <!-- Privacy -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-cyan-500"></span>
                  <p class="text-xs font-semibold text-cyan-400 uppercase tracking-wider">NSP — What Leaves the Device</p>
                </div>
                <p>Terminal sessions are local. Input commands and rendered output never leave the device unencrypted. When external data is needed, NSP wraps the request in post-quantum encryption (ML-KEM-768 key exchange, ML-DSA signatures). Responses are verified by Neurowall before rendering. The patient sees only authenticated content.</p>
              </div>

              <!-- Blockchain -->
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <span class="w-2 h-2 rounded-full bg-amber-500"></span>
                  <p class="text-xs font-semibold text-amber-400 uppercase tracking-wider">Blockchain — Tamper-Proof Access Logs</p>
                </div>
                <p>Every terminal session, every configuration change, every access event is logged to a blockchain-based chain of evidence. Patients can audit exactly what accessed their device, when, and what changed. If a misconfiguration occurs — accidental or malicious — the blockchain provides version-controlled rollback to any previous verified kernel state. Think git for your neural interface hardware.</p>
              </div>

              <!-- Security implications -->
              <div class="pt-4 border-t border-[rgba(59,130,246,0.1)]">
                <p class="text-xs font-semibold text-[var(--color-text-primary)] uppercase tracking-wider mb-2">Security Architecture</p>
                <ul class="space-y-2 text-xs">
                  <li class="flex items-start gap-2">
                    <span class="text-blue-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Emergency terminal</strong> — kernel-level fallback renders a minimal terminal via depth sensor data alone, independent of Runemate, NSP, or AI. If all software layers fail, the patient still has a basic interface</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-blue-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Configuration rollback</strong> — blockchain-versioned kernel states enable rollback to any prior verified configuration. No configuration change is permanent or unrecoverable. Every state transition is auditable</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-blue-500 mt-0.5 shrink-0">&#10003;</span>
                    <span><strong class="text-[var(--color-text-primary)]">Full patient agency</strong> — the terminal gives patients direct, unmediated control over their device. No intermediary application layer can prevent terminal access. This is cognitive liberty implemented as system architecture</span>
                  </li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Hero expand/collapse logic -->
  <script is:inline>
    (function() {
      document.querySelectorAll('[data-hero]').forEach(function(hero) {
        hero.addEventListener('click', function() {
          var variant = hero.getAttribute('data-hero');
          var detail = document.getElementById('detail-' + variant);
          var other = document.getElementById('detail-' + (variant === 'green' ? 'white' : 'green'));
          if (other && !other.classList.contains('hidden')) {
            other.classList.add('hidden');
          }
          if (detail) {
            detail.classList.toggle('hidden');
            if (!detail.classList.contains('hidden')) {
              detail.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
          }
        });
        hero.addEventListener('keydown', function(e) {
          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); hero.click(); }
        });
      });
      document.querySelectorAll('[data-close]').forEach(function(btn) {
        btn.addEventListener('click', function() {
          var variant = btn.getAttribute('data-close');
          var detail = document.getElementById('detail-' + variant);
          if (detail) detail.classList.add('hidden');
        });
      });
    })();
  </script>


  <!-- ═══ HOOK: The surprise ═══ -->
  <section class="relative min-h-[80vh] flex items-center justify-center py-24 sm:py-32">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="space-y-8 text-lg sm:text-xl text-[var(--color-text-muted)] leading-relaxed">
        <p class="reveal-elastic">I was cataloguing ways to attack a brain-computer interface.</p>
        <p class="reveal-elastic" style="transition-delay: 150ms;">Somewhere around technique 40, the catalog started reading differently.</p>
        <p class="reveal-elastic" style="transition-delay: 300ms;"><span class="text-[var(--color-text-primary)] font-semibold">Signal injection</span> — where an attacker sends crafted signals through an electrode — uses the same physical mechanism as <span class="text-emerald-500 font-semibold">deep brain stimulation</span>. The therapy that treats Parkinson's in over 160,000 patients.</p>
        <p class="reveal-elastic" style="transition-delay: 450ms;"><span class="text-[var(--color-text-primary)] font-semibold">Neural entrainment manipulation</span> — forcing the brain into an unnatural frequency — is the same mechanism as <span class="text-emerald-500 font-semibold">therapeutic tACS</span> for depression.</p>
        <p class="reveal-elastic text-2xl sm:text-3xl font-semibold" style="transition-delay: 600ms;">
          Same electrode. Same current. Same physics.<br/>
          <span class="bg-gradient-to-r from-red-500 via-amber-500 to-emerald-500 bg-clip-text text-transparent">Different intent.</span>
        </p>
        <p class="reveal-elastic mt-4" style="transition-delay: 750ms;"><a href="/research/papers/2026-02-09-tara-therapeutic-atlas-of-risks-and-applications/" class="text-xs text-[var(--color-accent-primary)] hover:underline">Read how the dual-use discovery unfolded &rarr;</a></p>
      </div>

      <div class="mt-16 grid grid-cols-1 sm:grid-cols-3 gap-6 max-w-2xl mx-auto reveal">
        <div class="text-center"><p class="text-4xl sm:text-5xl font-bold text-[var(--color-accent-primary)]"><ScrollCounter client:idle end={totalThreats} suffix="+" /></p><p class="text-xs text-[var(--color-text-muted)] mt-2">techniques catalogued</p></div>
        <div class="text-center"><p class="text-4xl sm:text-5xl font-bold text-emerald-500"><ScrollCounter client:idle end={therapeuticCount} /></p><p class="text-xs text-[var(--color-text-muted)] mt-2">with clinical analogs</p></div>
        <div class="text-center"><p class="text-4xl sm:text-5xl font-bold text-amber-500"><ScrollCounter client:idle end={dualUsePercent} suffix="%" /></p><p class="text-xs text-[var(--color-text-muted)] mt-2">dual-use overlap</p></div>
      </div>
    </div>
  </section>


  <!-- ═══ THE CROSSING ═══ -->
  <section class="relative py-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="space-y-8 text-lg sm:text-xl text-[var(--color-text-muted)] leading-relaxed">
        <p class="reveal-elastic">I was looking at a diagram of the thalamus when it hit me.</p>
        <p class="reveal-elastic" style="transition-delay: 120ms;">Every signal entering the cortex passes through this one relay. If you compromise it, you control what a person perceives. I had built the exact same chokepoint — in network security. A SIEM correlation engine. All signals in, one classification out. Compromise the engine, and the analyst sees whatever you want them to see.</p>
        <p class="reveal-elastic" style="transition-delay: 240ms;">That wasn't a metaphor. That was architecture.</p>
      </div>

      <div class="mt-12 glass rounded-xl p-6 reveal" style="border-color: rgba(59,130,246,0.15);">
        <div class="grid sm:grid-cols-3 gap-4 text-center text-sm">
          <div>
            <p class="text-[var(--color-text-primary)] font-semibold mb-1">Attack the spinal cord</p>
            <p class="text-xs text-[var(--color-text-faint)]">Involuntary movement</p>
          </div>
          <div>
            <p class="text-[var(--color-text-primary)] font-semibold mb-1">Attack the thalamus</p>
            <p class="text-xs text-[var(--color-text-faint)]">Altered perception</p>
          </div>
          <div>
            <p class="text-[var(--color-text-primary)] font-semibold mb-1">Attack the neocortex</p>
            <p class="text-xs text-[var(--color-text-faint)]">Thought extraction</p>
          </div>
        </div>
        <p class="text-center text-xs text-[var(--color-text-faint)] mt-4">Each layer has a distinct threat profile because each brain region has a distinct function.</p>
      </div>

      <div class="mt-16 space-y-6 text-[var(--color-text-muted)] leading-relaxed reveal">
        <p>AI already proved that other disciplines can transform neuroscience. Deep learning decodes speech at 62 words per minute. Neural networks reconstruct vision from electrode arrays. The understanding problem is being solved.</p>
        <p>But understanding is not protection. And the same AI that enables healing enables attack.</p>
        <div class="grid sm:grid-cols-2 gap-4 mt-6">
          <div class="rounded-xl p-5" style="background: rgba(139,92,246,0.06); border: 1px solid rgba(139,92,246,0.15);">
            <p class="text-[10px] font-mono tracking-[0.15em] uppercase text-[var(--color-accent-tertiary)] mb-3">What AI built</p>
            <p class="text-sm">Speech decoding. Vision restoration. Subvocal thought detection. Neural trajectory synthesis.</p>
          </div>
          <div class="rounded-xl p-5" style="background: rgba(239,68,68,0.06); border: 1px solid rgba(239,68,68,0.15);">
            <p class="text-[10px] font-mono tracking-[0.15em] uppercase text-red-500 mb-3">What nobody built</p>
            <p class="text-sm">Zero BCI threat models. No neural severity scoring. No MITRE entries. No encryption standard.</p>
          </div>
        </div>
      </div>

      <p class="mt-12 text-center reveal">
        <span class="text-lg text-[var(--color-text-muted)]">HTTPS had to exist before e-commerce. TLS had to exist before online banking.</span><br/>
        <span class="text-xl text-[var(--color-text-primary)] font-semibold">So I started building it.</span>
      </p>
    </div>
  </section>


  <!-- ═══ THE DISCOVERY: Attack ↔ Therapy ═══ -->
  <section class="relative py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto mb-12 reveal">
        <p class="text-[var(--color-text-muted)] leading-relaxed mb-6">A threat registry that is also a capabilities catalog for medicine. I did not plan this. The physics showed it.</p>
        <details class="glass rounded-xl overflow-hidden mt-2" style="border-color: rgba(245,158,11,0.15); background: rgba(245,158,11,0.03);">
          <summary class="cursor-pointer px-6 py-4 flex items-center gap-3 text-[var(--color-text-primary)] font-semibold select-none hover:bg-[rgba(245,158,11,0.05)] transition-colors list-none [&::-webkit-details-marker]:hidden">
            <span class="text-amber-500 transition-transform duration-300 text-xs tara-chevron">&#9654;</span>
            <span>Why the name Tara? (तारा)</span>
          </summary>
          <div class="px-6 pb-5 pt-3 border-t border-[rgba(245,158,11,0.1)]">
            <p class="text-[var(--color-text-muted)] leading-relaxed mb-4"><span class="text-[var(--color-text-primary)] font-semibold">Therapeutic Atlas of Risks and Applications.</span> Therapeutics leads. "Atlas" signals exploration. "Risks" instead of "exploits" — the people funding this work should see a map, not a weapon.</p>
            <p class="text-[var(--color-text-muted)] leading-relaxed"><span class="text-amber-500">Tara</span> (तारा) means "star" in Sanskrit. In Tibetan Buddhism, Tara is the bodhisattva of compassion and protection. Therapeutic use is the default. Adversarial use is the deviation — following the IAEA model where peaceful use is presumed.</p>
          </div>
        </details>
      </div>

      <!-- Attack ↔ Therapy visualization -->
      <div class="max-w-3xl mx-auto reveal">
        <div class="grid grid-cols-1 sm:grid-cols-[1fr_auto_1fr] gap-0 items-stretch">
          <div class="rounded-t-xl sm:rounded-t-none sm:rounded-l-xl p-5 space-y-4" style="background: rgba(239,68,68,0.06); border: 1px solid rgba(239,68,68,0.15);">
            <p class="text-[10px] font-mono tracking-[0.15em] uppercase text-red-500 mb-3">Attack Vector</p>
            {[
              { name: 'Signal Injection', desc: 'Spoof identity via electrode' },
              { name: 'Neural Entrainment', desc: 'Force oscillatory state' },
              { name: 'Bifurcation Forcing', desc: 'Push past tipping point' },
            ].map((t) => (
              <div><p class="text-xs font-semibold text-red-400">{t.name}</p><p class="text-[10px] text-[var(--color-text-faint)]">{t.desc}</p></div>
            ))}
          </div>
          <div class="flex flex-row sm:flex-col items-center justify-center py-3 sm:py-0 px-6 sm:px-6 relative" style="background: rgba(245,158,11,0.04);">
            <div class="h-px w-full sm:h-full sm:w-px absolute" style="background: linear-gradient(to right, rgba(239,68,68,0.3), rgba(245,158,11,0.5), rgba(16,185,129,0.3));"></div>
            <div class="relative z-10 bg-[var(--color-bg-deep)] rounded-lg px-3 py-2 sm:px-2 sm:py-3 text-center border border-amber-500/20">
              <p class="text-[9px] font-mono tracking-wider uppercase text-amber-500 sm:mb-0">Same Physics</p>
            </div>
          </div>
          <div class="rounded-b-xl sm:rounded-b-none sm:rounded-r-xl p-5 space-y-4" style="background: rgba(16,185,129,0.06); border: 1px solid rgba(16,185,129,0.15);">
            <p class="text-[10px] font-mono tracking-[0.15em] uppercase text-emerald-500 mb-3">Therapeutic Application</p>
            {[
              { name: 'Deep Brain Stimulation', desc: '160,000 Parkinson\'s patients' },
              { name: 'Therapeutic tACS', desc: 'Depression modulation' },
              { name: 'Controlled DBS', desc: 'Neural attractor correction' },
            ].map((t) => (
              <div><p class="text-xs font-semibold text-emerald-400">{t.name}</p><p class="text-[10px] text-[var(--color-text-faint)]">{t.desc}</p></div>
            ))}
          </div>
        </div>
      </div>

      <p class="text-center text-sm text-[var(--color-text-faint)] italic mt-8 reveal">A clinician and a red teamer look at the same electrode. One sees a treatment. The other sees an attack surface. The atlas shows both.</p>
    </div>
  </section>


  <!-- ═══ THE PIPELINE: Vision restoration ═══ -->
  <section class="relative py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto mb-12 reveal">
        <p class="text-xl sm:text-2xl text-[var(--color-text-primary)] font-semibold mb-6">The vision that won't let me sleep.</p>
        <p class="text-[var(--color-text-muted)] leading-relaxed mb-4">A lightweight rendering protocol that runs entirely on-device. Camera to local AI to spatial encoding to post-quantum encryption to BCI to visual cortex. All local. No cloud.</p>
        <p class="text-[var(--color-text-muted)] leading-relaxed mb-4">Nobody knows what Web 5.0 looks like. But there will always be terminals. White phosphenes are the simplest, most reliable way to render a neural terminal — giving patients not just vision, but full interactive control over their AI, their data, and their access logs.</p>
        <p class="text-lg text-[var(--color-text-primary)]">Person "sees" again. And they own what they see.</p>
      </div>

      <!-- Pipeline flow -->
      <div class="max-w-3xl mx-auto mb-4" id="pipeline-flow">
        <div class="flex flex-col gap-0">
          {[
            { label: 'Depth Sensor + AI', band: 'S3', desc: 'Camera captures environment, AI reconstructs scene. The sensor itself is the hardware failsafe — it operates independently of AI, so if the model fails, raw depth data still flows', color: '#3b82f6', consent: 'What is captured' },
            { label: 'Runemate Compiler', band: 'S2', desc: 'Compresses for neural delivery, on-device, 200 KB — like HTML but for neural signals', color: '#8b5cf6', consent: 'What is compiled' },
            { label: 'NSP Encryption', band: 'S1', desc: 'Post-quantum security envelope — ML-KEM-768 key exchange, five defense layers', color: '#06b6d4', consent: 'What is encrypted' },
            { label: 'Neurowall', band: 'I0', desc: 'Hardware-level signal integrity — amplitude bounds, rate limiting, immutable kernels', color: '#f59e0b', consent: 'What passes through' },
            { label: 'Visual Cortex', band: 'N7', desc: 'Patient perceives the world', color: '#22c55e', consent: 'What is perceived' },
          ].map((node, i) => (
            <div class="flex items-stretch gap-4 group pipeline-node" data-pipeline-index={i}>
              <div class="hidden sm:flex w-28 sm:w-36 shrink-0 items-center justify-end">
                <p class="text-[9px] font-mono text-right tracking-wider uppercase" style={`color: ${node.color}60;`}>{node.consent}</p>
              </div>
              <div class="flex flex-col items-center">
                <div class="w-3 h-3 rounded-full ring-2 shrink-0 pipeline-dot" style={`background: ${node.color}; ring-color: ${node.color}40;`}></div>
                {i < 4 && <div class="w-px flex-1 min-h-[40px] pipeline-line" style={`background: linear-gradient(to bottom, ${node.color}40, ${['#8b5cf6','#06b6d4','#f59e0b','#22c55e'][i]}40);`}></div>}
              </div>
              <div class="flex-1 pb-6">
                <div class="flex items-center gap-2 mb-1">
                  <span class="font-mono text-[10px]" style={`color: ${node.color};`}>{node.band}</span>
                  <span class="text-sm font-semibold text-[var(--color-text-primary)]">{node.label}</span>
                </div>
                <p class="text-xs text-[var(--color-text-faint)]">{node.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <p class="text-center text-sm text-[var(--color-text-muted)] max-w-2xl mx-auto mb-12 reveal italic">Security engineers already know how to build this. Signal integrity. Threat detection. Rate limiting. Authentication. The skills transfer directly. The substrate is new — the architecture isn't.</p>
    </div>
  </section>


  <!-- ═══ THE FAILSAFE: What happens when it fails ═══ -->
  <section class="relative py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto mb-12 reveal">
        <p class="text-[var(--color-text-muted)] leading-relaxed mb-6">AI engineers optimize for capability. Security engineers optimize for what happens when capability fails. In neural interfaces, that difference is the difference between a patient who sees and a patient who is locked in.</p>
      </div>

      <!-- Failsafe architecture -->
      <div class="max-w-3xl mx-auto mb-12 reveal">
        <div class="relative">
          <div class="absolute left-6 top-0 bottom-0 w-px" style="background: linear-gradient(to bottom, #3b82f6, #8b5cf6, #06b6d4, #f59e0b, #22c55e);"></div>
          <div class="space-y-6">
            {[
              { layer: 'AI Vision', band: 'S3', status: 'Can fail', desc: 'If the AI hallucinates, the sensor\'s raw depth data provides a baseline.', color: '#3b82f6', safe: false },
              { layer: 'Runemate', band: 'S2', status: 'Can fail', desc: 'If the compiler fails, no signal passes through I0. Fail-closed.', color: '#8b5cf6', safe: false },
              { layer: 'NSP', band: 'S1', status: 'Can fail', desc: 'If encryption is compromised, Neurowall rejects unverified packets.', color: '#06b6d4', safe: false },
              { layer: 'Neurowall', band: 'I0', status: 'Cannot be overridden', desc: 'Amplitude bounds are physical. Rate limiting at silicon. Immutable kernels with blockchain-verified integrity — every state change logged to a tamper-proof chain of evidence. No remote update touches these.', color: '#f59e0b', safe: true },
              { layer: 'Patient', band: 'N1-N7', status: 'Full agency', desc: 'Every software layer can fail. No failure overrides the hardware envelope. Blockchain access logs give patients verifiable proof of what touched their device and when.', color: '#22c55e', safe: true },
            ].map((item) => (
              <div class="flex items-start gap-4 pl-2">
                <div class={`shrink-0 w-3 h-3 rounded-full mt-2 ring-2 ${item.safe ? 'ring-4' : ''}`} style={`background: ${item.color}; ring-color: ${item.color}30;`}></div>
                <div class="flex-1 glass rounded-xl p-5" style={`border-color: ${item.color}20;`}>
                  <div class="flex items-center gap-3 mb-2 flex-wrap">
                    <span class="text-sm font-semibold" style={`color: ${item.color};`}>{item.layer}</span>
                    <span class="font-mono text-[10px] text-[var(--color-text-faint)]">{item.band}</span>
                    <span class={`text-[10px] font-semibold px-2 py-0.5 rounded-full ${item.safe ? 'bg-emerald-500/10 text-emerald-500' : 'bg-red-500/10 text-red-400'}`}>{item.status}</span>
                  </div>
                  <p class="text-sm text-[var(--color-text-muted)]">{item.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      <div class="max-w-3xl mx-auto text-center reveal">
        <div class="glass rounded-2xl p-8" style="border-color: rgba(6,182,212,0.3);">
          <p class="text-lg sm:text-xl text-[var(--color-text-primary)] font-semibold leading-relaxed mb-4">NSP isn't the wall around the castle.<br/>NSP is the road that lets the ambulance through.</p>
          <p class="text-sm text-emerald-500 font-semibold mt-2">Security enables medicine. That's the whole point.</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ═══ THE STAKES: Why this can't wait ═══ -->
  <section class="relative py-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="space-y-8 text-[var(--color-text-muted)] leading-relaxed reveal">
        <p class="text-lg"><strong class="text-[var(--color-text-primary)]">57 million people</strong> globally live with dementia. The brain has systems that survive what other systems can't — procedural memory persists when explicit memory is gone.</p>
        <p class="text-lg">If a BCI could one day support hippocampal function — help form new memories, reinforce fading ones — the security around that intervention would need to be absolute.</p>
        <p class="text-xl sm:text-2xl font-semibold text-[var(--color-text-primary)]">You don't get to be careless with someone's last remaining memories.</p>
        <p class="mt-4"><a href="/research/papers/2026-02-09-field-journal-008-she-never-forgot-how-to-pray/" class="text-xs text-[var(--color-accent-primary)] hover:underline">Read Field Journal #008 &rarr;</a></p>
      </div>

      <div class="mt-12 glass rounded-2xl p-8 reveal" style="border-color: rgba(245,158,11,0.25); background: rgba(245,158,11,0.03);">
        <p class="text-lg text-[var(--color-text-primary)] font-semibold leading-relaxed mb-3">Right now, BCI governance is like writing fire codes without building fire hydrants or fire stations.</p>
        <p class="text-sm text-[var(--color-text-muted)] leading-relaxed">The neuroethics community has defined the rights. Legislators are passing laws. But nobody has built the technical infrastructure to enforce any of it. Fire codes and fire stations need to be built together — one without the other protects no one.</p>
      </div>

      <div class="mt-12 glass rounded-2xl p-8 reveal" style="border-color: rgba(239,68,68,0.2);">
        <p class="text-[var(--color-text-muted)] leading-relaxed">A single high-profile brainjacking incident will not just set back one company. <strong class="text-[var(--color-text-primary)]">It will poison the well for the entire field.</strong> Every therapeutic application — Parkinson's, vision restoration, depression — depends on public trust. That trust does not survive the first headline about a neural implant being compromised.</p>
      </div>
    </div>
  </section>


  <!-- ═══ KNIGHT'S WATCH ═══ -->
  <section class="relative py-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12 reveal">
        <p class="text-xs font-mono tracking-[0.15em] uppercase text-amber-500 mb-4">Why Open Source?</p>
        <h2 class="text-2xl sm:text-3xl font-bold mb-6">Sometimes the best security defense is a deterrent.</h2>
      </div>

      <div class="space-y-6 text-[var(--color-text-muted)] leading-relaxed reveal">
        <p>You've seen community watch signs in neighborhoods. No cameras. No guards. Just a sign. And it works — because the knowledge that a community is watching changes the calculus for anyone who means harm.</p>
        <p class="text-lg text-[var(--color-text-primary)] font-semibold">Knight's Watch is that sign for the neural age.</p>
        <p>Every BCI device with vision restoration already carries depth sensors and environmental awareness. With the patient's explicit opt-in consent, that awareness can contribute to something larger — a community mesh where anonymized, hashed environmental metadata helps locate missing children and disrupt trafficking networks.</p>
      </div>

      <div class="mt-10 glass rounded-2xl p-8 reveal" style="border-color: rgba(245,158,11,0.2);">
        <div class="flex items-center gap-3 mb-6">
          <span class="text-2xl">&#9876;</span>
          <h3 class="text-lg font-bold text-amber-500">Knight's Watch</h3>
          <span class="text-[10px] font-mono px-2 py-0.5 rounded-full border border-amber-500/30 text-amber-500/70">CONCEPT</span>
        </div>

        <div class="space-y-4 text-sm text-[var(--color-text-muted)]">
          <div class="flex items-start gap-3">
            <span class="text-emerald-500 mt-0.5 shrink-0 font-bold">1</span>
            <div>
              <p class="font-semibold text-[var(--color-text-primary)] mb-1">Opt-in only. Always.</p>
              <p>No patient is enrolled by default. Participation is an explicit, revocable choice — cognitive liberty applies here too. The patient decides what their device contributes and can withdraw at any time.</p>
            </div>
          </div>

          <div class="flex items-start gap-3">
            <span class="text-emerald-500 mt-0.5 shrink-0 font-bold">2</span>
            <div>
              <p class="font-semibold text-[var(--color-text-primary)] mb-1">Privacy first. No raw data leaves the device.</p>
              <p>Sensors capture depth and spatial data locally. AI processes object recognition on-device. Only hashed, anonymized metadata — never imagery, never location, never identity — is transmitted through NSP's post-quantum encryption. What the patient sees stays with the patient.</p>
            </div>
          </div>

          <div class="flex items-start gap-3">
            <span class="text-emerald-500 mt-0.5 shrink-0 font-bold">3</span>
            <div>
              <p class="font-semibold text-[var(--color-text-primary)] mb-1">COPPA-compliant by architecture.</p>
              <p>Child protection isn't a policy layer bolted on after — it's a design constraint from the start. No biometric data on minors is collected, stored, or transmitted. The system detects patterns associated with trafficking and missing persons alerts without ever identifying or profiling a child.</p>
            </div>
          </div>

          <div class="flex items-start gap-3">
            <span class="text-emerald-500 mt-0.5 shrink-0 font-bold">4</span>
            <div>
              <p class="font-semibold text-[var(--color-text-primary)] mb-1">The deterrent is the point.</p>
              <p>A community watch sign works because criminals know the neighborhood is watching. Knight's Watch works the same way. The knowledge that a mesh of neural-enabled citizens exists — privacy-preserving, legally compliant, community-governed — changes the risk calculus for traffickers. The sign on the wall matters as much as the camera behind it.</p>
            </div>
          </div>

          <div class="flex items-start gap-3">
            <span class="text-emerald-500 mt-0.5 shrink-0 font-bold">5</span>
            <div>
              <p class="font-semibold text-[var(--color-text-primary)] mb-1">Blockchain-auditable participation.</p>
              <p>Every opt-in, every metadata contribution, every alert is logged to the same tamper-proof chain of evidence that protects kernel configurations. Patients can verify their participation history. Law enforcement can verify alert provenance. Nobody can fabricate or tamper with the record.</p>
            </div>
          </div>
        </div>

        <div class="mt-6 pt-4 border-t border-amber-500/10">
          <p class="text-xs text-[var(--color-text-faint)]">Knight's Watch is a proposed concept, not a deployed system. Full technical specification, threat model, and compliance analysis are in development. The architecture inherits NSP encryption, Neurowall signal integrity, and Runemate's on-device processing — no new trust assumptions beyond the existing QIF stack.</p>
        </div>
      </div>

      <p class="text-center text-sm text-[var(--color-text-faint)] mt-8 italic reveal">A world with a little less fear. That's the community we can build together.</p>
    </div>
  </section>


  <!-- ═══ THE CLOSE ═══ -->
  <section class="relative py-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="space-y-8 text-[var(--color-text-muted)] leading-relaxed mb-12">
        <p class="text-lg reveal-elastic">This work does not fit neatly into cybersecurity, neuroscience, or biomedical engineering.</p>
        <p class="text-lg reveal-elastic" style="transition-delay: 100ms;">It is the intersection of all three, applied to devices that are already in human skulls. We are calling it <span class="text-emerald-500 font-semibold">neurosecurity engineering</span> — securing the interface between silicon and the human nervous system, using the same physics that makes attacks possible to make therapies safe.</p>
        <p class="text-xl font-semibold text-[var(--color-text-primary)] reveal-elastic" style="transition-delay: 200ms;">This cannot be built alone.</p>
      </div>

      <!-- Call for collaborators -->
      <div class="glass rounded-2xl p-8 mb-16 reveal" style="border-color: rgba(16,185,129,0.2);">
        <h3 class="text-lg font-bold text-emerald-500 mb-6">If this resonates, there's a place for you here.</h3>
        <div class="grid sm:grid-cols-2 gap-4 text-sm text-[var(--color-text-muted)]">
          <div>
            <p class="font-semibold text-[var(--color-text-primary)] mb-1">Security engineers</p>
            <p>Red-team the framework. Find what we missed. Build the detections. The threat atlas has {totalThreats} techniques — every one of them needs validation against real hardware.</p>
          </div>
          <div>
            <p class="font-semibold text-[var(--color-text-primary)] mb-1">Neuroscientists</p>
            <p>Validate the clinical mappings. Tell us where the anatomy is wrong, where the therapeutic analogues break down, where the scoring needs recalibration.</p>
          </div>
          <div>
            <p class="font-semibold text-[var(--color-text-primary)] mb-1">Patients and families</p>
            <p>You are who this is for. Your experience with BCI devices, your concerns about safety, your feedback on what informed consent should actually look like — that shapes everything.</p>
          </div>
          <div>
            <p class="font-semibold text-[var(--color-text-primary)] mb-1">Policymakers</p>
            <p>The legislative momentum is real. Chile, Colorado, California, Montana. The frameworks need technical infrastructure to enforce. We are building it.</p>
          </div>
        </div>
        <div class="mt-6 pt-4 border-t border-emerald-500/10 text-center">
          <a href="https://github.com/qinnovates/qinnovate" target="_blank" rel="noopener noreferrer" class="text-sm text-emerald-500 hover:text-emerald-400 transition-colors">Contribute on GitHub &rarr;</a>
        </div>
      </div>

      <div class="text-center reveal">
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-8">
          <a href="/research/whitepaper/" class="px-8 py-3.5 rounded-xl bg-[var(--color-accent-primary)] text-white font-medium hover:opacity-90 transition-opacity">Read the Whitepaper</a>
          <a href="/TARA/" class="px-8 py-3.5 rounded-xl glass font-medium hover:bg-white/10 transition-colors">Explore the Atlas &rarr;</a>
          <a href="/guardrails/" class="px-8 py-3.5 rounded-xl glass font-medium hover:bg-white/10 transition-colors">See the Specifications &rarr;</a>
        </div>
        <p class="font-mono text-[10px] text-[var(--color-text-faint)] tracking-[0.1em]">PROPOSED &middot; NOT VALIDATED &middot; OPEN TO CRITIQUE</p>
      </div>
    </div>
  </section>


  <!-- ═══ TRANSPARENCY ═══ -->
  <section class="py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-2 gap-6 reveal-stagger reveal">
        <div class="glass rounded-xl p-6 glow-border"><h3 class="text-lg font-bold mb-4">What This Work Is</h3><ul class="space-y-2 text-sm text-[var(--color-text-muted)]"><li>&#8250; Open-source research portfolio</li><li>&#8250; Proposed threat modeling methods</li><li>&#8250; Bridge between security and neurorights</li></ul></div>
        <div class="glass rounded-xl p-6 glow-border"><h3 class="text-lg font-bold mb-4">What This Work Is Not</h3><ul class="space-y-2 text-sm text-[var(--color-text-muted)]"><li>&times; Not a clinical or diagnostic tool</li><li>&times; Not an adopted standard</li><li>&times; Not independently validated</li></ul></div>
      </div>
    </div>
  </section>


  <script>
    document.addEventListener('astro:before-swap', (e: any) => {
      const nextUrl = e?.to?.url?.pathname || '';
      if (!nextUrl.startsWith('/vision')) {
        document.documentElement.setAttribute('data-theme', 'light');
        document.documentElement.setAttribute('data-vision', 'off');
      }
    });
  </script>

  <script>
    function initVisionMotion() {
      const elasticEls = document.querySelectorAll('.reveal-elastic');
      const elasticObs = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
            elasticObs.unobserve(entry.target);
          }
        });
      }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });
      elasticEls.forEach((el) => elasticObs.observe(el));

      // Pipeline flow scroll animation
      const pipelineNodes = document.querySelectorAll<HTMLElement>('.pipeline-node');
      if (pipelineNodes.length > 0) {
        const pipelineObs = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              const node = entry.target as HTMLElement;
              const index = parseInt(node.dataset.pipelineIndex || '0', 10);
              setTimeout(() => {
                node.classList.add('pipeline-lit');
              }, index * 200);
              pipelineObs.unobserve(node);
            }
          });
        }, { threshold: 0.2, rootMargin: '0px 0px -20px 0px' });
        pipelineNodes.forEach((node) => pipelineObs.observe(node));
      }

      document.querySelectorAll<HTMLElement>('.card-tilt').forEach((card) => {
        card.addEventListener('mousemove', (e) => {
          const rect = card.getBoundingClientRect();
          const x = (e.clientX - rect.left) / rect.width - 0.5;
          const y = (e.clientY - rect.top) / rect.height - 0.5;
          card.style.transform = `perspective(600px) rotateY(${x * 8}deg) rotateX(${-y * 8}deg) scale(1.02)`;
        });
        card.addEventListener('mouseleave', () => {
          card.style.transform = 'perspective(600px) rotateY(0deg) rotateX(0deg) scale(1)';
        });
      });
    }
    initVisionMotion();
    document.addEventListener('astro:after-swap', initVisionMotion);
  </script>

  <style>
    /* Tara naming collapsible chevron */
    details[open] .tara-chevron {
      transform: rotate(90deg);
    }

    /* Pipeline scroll animation */
    .pipeline-node {
      opacity: 0.3;
      transform: scale(0.97);
      transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .pipeline-node.pipeline-lit {
      opacity: 1;
      transform: scale(1);
    }

    .pipeline-node .pipeline-dot {
      transition: box-shadow 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .pipeline-node.pipeline-lit .pipeline-dot {
      box-shadow: 0 0 12px currentColor;
    }

    .pipeline-node .pipeline-line {
      opacity: 0;
      transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.3s;
    }

    .pipeline-node.pipeline-lit .pipeline-line {
      opacity: 1;
    }
  </style>

  <Footer />
  <BciVisionToggle client:idle />
</BaseLayout>
